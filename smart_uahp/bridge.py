"""Carbon-Silicon KV-handshake transport."""
# Stub — full implementation in progress.
class CarbonSiliconBridge:
    """Transfers compressed KV-tensor from Carbon (CPU/local) to Silicon (LPU/remote) substrate."""
    def send(self, kv_tensor: bytes, destination: str) -> bool:
        raise NotImplementedError
